# yaml-language-server: $schema=https://aka.ms/m365-agents-toolkits/v1.9/yaml.schema.json
# Visit https://aka.ms/teamsfx-v5.0-guide for details on this file
# Visit https://aka.ms/teamsfx-actions for details on actions
version: v1.9

environmentFolderPath: ./env

# Triggered when 'teamsapp provision' is executed
provision:
  # - uses: script
  #   name: set environment variables
  #   with:
  #     run: |
  #       $AZUREPRINCIPALID = az ad signed-in-user show --query id -o tsv
  #       Write-Host "::set-teamsfx-env AZURE_PRINCIPAL_ID=$AZUREPRINCIPALID"
  #     shell: pwsh

  # Creates an app
  - uses: teamsApp/create
    with:
      # app name
      name: spec-to-agents${{APP_NAME_SUFFIX}}
    # Write the information of created resources into environment file for
    # the specified environment variable(s).
    writeToEnvironmentFile:
      teamsAppId: TEAMS_APP_ID

  - uses: arm/deploy # Deploy given ARM templates parallelly.
    with:
      # AZURE_SUBSCRIPTION_ID is a built-in environment variable,
      # if its value is empty, TeamsFx will prompt you to select a subscription.
      # Referencing other environment variables with empty values
      # will skip the subscription selection prompt.
      subscriptionId: ${{AZURE_SUBSCRIPTION_ID}}
      # AZURE_RESOURCE_GROUP_NAME is a built-in environment variable,
      # if its value is empty, TeamsFx will prompt you to select or create one
      # resource group.
      # Referencing other environment variables with empty values
      # will skip the resource group selection prompt.
      resourceGroupName: ${{AZURE_RESOURCE_GROUP_NAME}}
      templates:
        - path: ./infra/main.bicep # Relative path to this file
          # Relative path to this yaml file.
          # Placeholders will be replaced with corresponding environment
          # variable before ARM deployment.
          parameters: ./infra/main.parameters.teams.json
          # Required when deploying ARM template
          deploymentName: Create-resources-for-bot
      # Microsoft 365 Agents Toolkit will download this bicep CLI version from github for you,
      # will use bicep CLI in PATH if you remove this config.
      bicepCliVersion: v0.39.26

  # Validate using manifest schema
  - uses: teamsApp/validateManifest
    with:
      # Path to manifest template
      manifestPath: ./appPackage/manifest.json
  # Build app package with latest env value
  - uses: teamsApp/zipAppPackage
    with:
      # Path to manifest template
      manifestPath: ./appPackage/manifest.json
      outputZipPath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
      outputFolder: ./appPackage/build
  # Validate app package using validation rules
  - uses: teamsApp/validateAppPackage
    with:
      # Relative path to this file. This is the path for built zip file.
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
  # Apply the app manifest to an existing app in
  # Developer Portal.
  # Will use the app id in manifest file to determine which app to update.
  - uses: teamsApp/update
    with:
      # Relative path to this file. This is the path for built zip file.
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
  - uses: teamsApp/extendToM365
    with:
      # Relative path to the build app package.
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
    # Write the information of created resources into environment file for
    # the specified environment variable(s).
    writeToEnvironmentFile:
      titleId: M365_TITLE_ID
      appId: M365_APP_ID

# Triggered when 'teamsapp deploy' is executed
deploy:
  # Run npm command
  - uses: script
    name: set environment variables
    with:
      run: echo "::set-teamsfx-env
        AZURE_CONTAINER_IMAGE_APP=${{AZURE_CONTAINER_REGISTRY_ENDPOINT}}/app:latest";
        echo "::set-teamsfx-env
        AZURE_CONTAINER_IMAGE_BOT=${{AZURE_CONTAINER_REGISTRY_ENDPOINT}}/bot:latest"

  - uses: script  
    name: build container image 1
    with:  
      run: az acr build --registry ${{AZURE_CONTAINER_REGISTRY_NAME}} --image ${{AZURE_CONTAINER_IMAGE_BOT}} --build-arg APP_CMD="m365-server" .;
        az acr build --registry ${{AZURE_CONTAINER_REGISTRY_NAME}} --image ${{AZURE_CONTAINER_IMAGE_APP}} --build-arg APP_CMD=app .

  - uses: script
    name: deploy container image
    with:
      run: |
        az containerapp update -n ${{AZURE_APP_NAME}} --image ${{AZURE_CONTAINER_IMAGE_APP}} -g ${{AZURE_RESOURCE_GROUP_NAME}} --revision-suffix ([int](Get-Date -UFormat %s -Millisecond 0));
        az containerapp update -n ${{AZURE_BOT_NAME}} --image ${{AZURE_CONTAINER_IMAGE_BOT}} -g ${{AZURE_RESOURCE_GROUP_NAME}} --revision-suffix ([int](Get-Date -UFormat %s -Millisecond 0))

# --env-vars "clientId=secretref:bot-id"
# "tenantId=secretref:bot-tenant-id" "BOT_TYPE=secretref:bot-type" "BOT_DOMAIN=${{BOT_DOMAIN}}"
# "AAD_APP_CLIENT_ID=secretref:aad-app-client-id"
# "AAD_APP_CLIENT_SECRET=secretref:aad-app-client-secret"
# "AAD_APP_TENANT_ID=secretref:aad-app-tenant-id"
# "AAD_APP_OAUTH_AUTHORITY_HOST=secretref:aad-app-oauth-authority-host"

# Triggered when 'teamsapp publish' is executed
publish:
  # Validate using manifest schema
  - uses: teamsApp/validateManifest
    with:
      # Path to manifest template
      manifestPath: ./appPackage/manifest.json
  # Build app package with latest env value
  - uses: teamsApp/zipAppPackage
    with:
      # Path to manifest template
      manifestPath: ./appPackage/manifest.json
      outputZipPath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
      outputFolder: ./appPackage/build
  # Validate app package using validation rules
  - uses: teamsApp/validateAppPackage
    with:
      # Relative path to this file. This is the path for built zip file.
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
  # Apply the app manifest to an existing app in
  # Developer Portal.
  # Will use the app id in manifest file to determine which app to update.
  - uses: teamsApp/update
    with:
      # Relative path to this file. This is the path for built zip file.
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
  # Publish the app to
  # Teams Admin Center (https://admin.teams.microsoft.com/policies/manage-apps)
  # for review and approval
  - uses: teamsApp/publishAppPackage
    with:
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
    # Write the information of created resources into environment file for
    # the specified environment variable(s).
    writeToEnvironmentFile:
      publishedAppId: TEAMS_APP_PUBLISHED_APP_ID
projectId: b426416d-ca20-4a22-81b3-b22044d71e5e
